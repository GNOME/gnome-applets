 > add support for panel size hints, will use clockmail as a guide.
   (theme.c is based on clockmail's theme.c, in fact most code started there)
 > manager.c has too much code duplication, needs attention.

